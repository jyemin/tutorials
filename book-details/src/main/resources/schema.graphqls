type Query {
    books: [Book]
    bookById(id: ID): Book
}

type Book {
    id: ID
    name: String
    pageCount: Int
    authors: [Author]
}

type Author {
    id: ID
    firstName: String
    lastName: String
}